% PREAMBLE LOADER
% Bleibt klein und lädt nur die modularen Style-Module aus styles/.
% Öffentliche Macros für Kapiteldateien werden in den Modulen definiert.
%
% ── Opt-in-Schalter ──────────────────────────────────────────────────
% Fachspezifische Module sind mitgeliefert, aber standardmässig AUS.
% Pro Fach hier (und NUR hier) einkommentieren, was gebraucht wird:
% Ausnahme: main.tex setzt im Template-Repository ZSFShowcaseMode, damit die
% Living Showcase alles rendert. init_project.sh entfernt diesen Marker.
%   11_math_advanced  → mathe-lastige Fächer (LinAlg, Analysis):
%                       Operatoren (Ker, rang, Spur, proj, …), Im/Re,
%                       TikZ-Klammern (\drawbrace/\tikzmark).
%   12_plots          → pgfplots für echte Plot-/Diagramm-Inhalte.
%   65_code_style     → code-lastige Fächer (Informatik): lstlisting +
%                       CodeExpert-Style + Code-Container (codebox).
%   66_index          → Stichwortverzeichnis am Dokumentende (\printindex);
%                       \ZSFkeyword indexiert dann automatisch.
%   67_code_comments  → Smart Inline/Overflow Code-Kommentare (\CodeLine).
%                       Unabhängig von 65: \CodeLine wirkt in jeder Textbox,
%                       nur NICHT in lstlisting — dort native #-Kommentare.
% Module nicht löschen — nur deaktivieren (siehe rules/10_architecture).
%
% =====================================================================
% GLOBALE STELLSCHRAUBEN — die oberste Ebene der Pyramide
% =====================================================================
% Das sind ALLE Gestaltungsentscheidungen, die pro ZSF getroffen werden
% dürfen (rules/02_ai_mandate → globale Stellschrauben). Was hier nicht
% steht, wird nicht pro ZSF entschieden, sondern pro Stelle über einen
% Box-Regler (rules/21_box_options) — oder gar nicht.
%
% Alle stehen auskommentiert mit ihrem Default. Verstellen heisst: Zeile
% einkommentieren und Wert ändern. Ein Register direkt zu setzen bleibt
% auch hier falsch — dafür gibt es die Faktoren.
%
% tests/check_global_knobs.sh prüft jede dieser Zeilen: Wer eine Schraube
% dreht, muss eine messbare Wirkung bekommen. Eine Stellschraube ohne
% Wirkung bricht den Build.
% ─────────────────────────────────────────────────────────────────────

% ── Platz: Dichte, Zeilenhöhe, Umbruchreserve ────────────────────────
% Müssen VOR styles/30_layout_spacing stehen.
% Dichte und Zeilenhöhe sind getrennt, weil unterschiedlich riskant:
% Abstände sind Leerraum und vertragen jede Skalierung, Zeilenhöhe
% enthält die Schrift selbst — nur in kleinen Schritten senken und
% danach das PDF auf kollidierende Formelzeilen prüfen.
% \def\ZSFDensityFactor{0.85}     % 1    = Referenz, kleiner = dichter
% \def\ZSFLeadingFactor{0.92}     % 0.95 = Referenz, kleiner = enger
%
% Bereichsfaktoren multiplizieren den globalen Faktor für IHREN Bereich —
% für ZSF, die ungleich verteilt sind (box-lastig vs. textlastig).
% \def\ZSFDensityBoxes{1}         % Innen-/Aussenabstände der Inhaltsboxen
% \def\ZSFDensityText{1}          % Absatzabstand im Fliesstext
% \def\ZSFDensityTables{1}        % Zell- und Zeilenabstand in Tabellen
% \def\ZSFDensityStructure{1}     % Polsterung und Abstände der Titelbalken
%
% Freiraum, der vor einem Titelbalken übrig sein muss, sonst bricht die
% Spalte davor um. Grösser = Balken stehen seltener knapp am Spaltenfuss.
% \def\ZSFBreakReserveFactor{1}   % 1 = Referenz
%
% Der Abstand NACH einem Titelbalken — der ganze Abstand bis zum ersten
% Block darunter, Fliesstext wie Box. Eigener Regler und nicht Teil von
% \ZSFDensityStructure: Wie eng eine Überschrift an ihrem Inhalt klebt, ist
% eine Bindungs-Entscheidung und keine Frage der Gesamtdichte — in einer
% ZSF mit vielen kurzen Abschnitten will man ihn enger als alles andere.
% \def\ZSFBarGapFactor{1}         % 1 = Referenz (3pt), kleiner = enger
%
% Schriftgrösse der Diagramm-Beschriftung — beide Stufen gemeinsam
% (\ZSFfontDiagramLabel und \ZSFfontDiagramLabelSmall). Kleiner für Fächer mit
% vielen gedrängten Zeichnungen; die zwei Stufen bleiben dabei unterscheidbar.
% Der einzige Schrift-Regler des Systems: Fliesstext- und Titelgrössen sind
% aufeinander abgestimmt und werden nicht pro ZSF gedreht.
% \def\ZSFDiagramLabelScale{0.85} % 1 = Referenz, kleiner = kleinere Labels

\input{styles/00_packages}
\input{styles/10_math}

% ── Formelsatz: Summen- und Limes-Grenzen ────────────────────────────
% side = neben dem Zeichen (spart Höhe), stack = darüber/darunter,
% auto = LaTeX entscheidet nach Kontext.
% \SetZSFsumMode{auto}            % auto | side | stack
% \SetZSFlimMode{auto}            % auto | side | stack

\ifdefined\ZSFShowcaseMode
  \input{styles/11_math_advanced}
  \input{styles/12_plots}
\else
  % \input{styles/11_math_advanced} % opt-in: LinAlg/Analysis
  % \input{styles/12_plots}         % opt-in: pgfplots-Diagramme
\fi
\input{styles/20_tables}

% ── Tabellen: Zebra-Ton ──────────────────────────────────────────────
% Default folgt der Kapitelfarbe. Ein fester Ton macht Tabellen über alle
% Kapitel hinweg gleich — sinnvoll, wenn die Kapitelfarbe schon woanders trägt.
% \SetZSFzebraBG{\TableZebraColor}

\input{styles/30_layout_spacing}
\input{styles/40_colors_structure}
\input{styles/50_typography_semantics}

% ── Marker-Darstellung ───────────────────────────────────────────────
% Default: \ZSFkeyword und \ZSFlabel beide schlicht fett, ohne Farbe.
% Inline-Betonung im Fliesstext trägt bewusst KEINE Kapitelfarbe — sie brächte
% Unruhe in den Absatz und sagte nichts, was die Seite nicht ohnehin zeigt.
% Farbe bleibt den Flächen, den Verweisen und den Formel-Markern vorbehalten.
% Umstellen heisst GENAU diese eine Zeile, nie ein Kapitel:
% \renewcommand{\ZSFkeywordStyle}[1]{\textbf{#1}}
% \renewcommand{\ZSFlabelStyle}[1]{\textbf{#1}}
% Dritter Marker derselben Familie: die Hervorhebung der Kernaussage.
% \renewcommand{\ZSFhlStyle}[1]{{\bfseries #1}}

% ── Grössenfarben: Farbe als Identität einer Grösse ──────────────────
% Für Fächer, in denen dieselben Grössen über das ganze Dokument hinweg in
% Formeln auftauchen (Kraft, Moment, Spannung, Durchmesser …). Eine Farbe
% gehört dann im ganzen Dokument EINER Grösse — beim Zusammensetzen einer
% Formel sieht man sofort, welche Grösse wo landet.
%
% Nicht zu verwechseln mit \ZSFmhlA..D: Die sind POSITIONAL (»erster Strang«,
% »Ziel«) und gelten innerhalb einer Herleitung.
%
% Zwölf Slots; ein doppelt vergebener bricht ab (eine Farbe, die zwei Grössen
% bedeutet, sagt nichts mehr aus). Wo das Fach keine wiederkehrenden Grössen
% hat, bleibt der Block leer und es ändert sich nichts.
% \ZSFDeclareQuantity{Kraft}{1}   % danach in Kapiteln: \ZSFqKraft{F}
% \ZSFDeclareQuantity{Moment}{5}  % danach:              \ZSFqMoment{M}
%
% Hauptschalter — nimmt alle Grössenfarben zurück (z.B. für S/W-Druck),
% ohne die Deklarationen zu entfernen:
% \ZSFQuantityColorsOff

% ── Eigener Ton: eine wiederkehrende Farbwelt fürs Fach ──────────────
% `tone` hat drei mitgelieferte Werte (chapter, neutral, warn). Braucht das
% Fach eine WIEDERKEHRENDE visuelle Kategorie, die keiner davon trägt (ein
% Beispiel-Block, ein Merksatz), ist die Antwort ein weiterer Ton — keine
% weitere Box. Nach der Deklaration gilt `tone=beispiel` auf JEDER Box.
%
% Alle neun Rollen sind Pflicht: Eine offene Rolle bricht mit einer Meldung ab,
% statt still die Farbe eines fremden Tons zu erben.
%
% Sparsam — ein Ton bedeutet im ganzen Dokument dasselbe und ist kein Weg, einer
% einzelnen Box eine Wunschfarbe zu geben.
% \ZSFDeclareTone{beispiel}{
%   accent       = ChapterColor3,   titleback   = ChapterColor3,
%   titletext    = white,           loudback    = ChapterColor3Light!12,
%   quietback    = ChapterColor3Light!12,
%   emphasisback = ChapterColor3Light!30,
%   framesoft    = ChapterColor3Light!35,
%   framestrong  = ChapterColor3,   framehard   = ChapterColor3,
% }
\ifdefined\ZSFShowcaseMode
  % Nur für die Living Showcase: init_project.sh entfernt den Marker, ein
  % Fach-Fork startet damit ohne Deklarationen.
  \ZSFDeclareQuantity{Kraft}{1}
  \ZSFDeclareQuantity{Weg}{2}
  \ZSFDeclareQuantity{Moment}{5}
\fi

\input{styles/55_readability}

% ── Fliesstext: Flattersatz ──────────────────────────────────────────
% \ZSFReadableBodyOn ist der Default, weil die ~50 mm schmalen Spalten im
% Blocksatz entweder trennen oder Wortzwischenräume aufblähen müssen.
% \ZSFReadableBodyOff             % zurück zu Blocksatz

\input{styles/60_boxes}
\ifdefined\ZSFShowcaseMode
  \input{styles/65_code_style}
  \input{styles/66_index}
  \ZSFIndexShowPageNumbertrue       % Template-Standard: Abschnitt · (S. Seite)
  \input{styles/67_code_comments}
\else
  % \input{styles/65_code_style}    % opt-in: Informatik
  % \input{styles/66_index}         % opt-in: Stichwortverzeichnis
  % \input{styles/67_code_comments} % opt-in: annotierte Code-Zeilen
  %
  % ── Index-Locator (nur mit aktivem 66_index) ───────────────────────
  % true (Default): "6.2 · (S. 4)" — Abschnittsnummer plus Druckseite.
  % false: nur die Abschnittsnummer.
  % \ZSFIndexShowPageNumberfalse
\fi
\input{styles/75_pdf_identity}
\input{styles/70_document_settings}
